// examples/demo.ltl — runnable demo for ltl_chacha20

import std::io
use ltl_chacha20 as lib

fn main() {
    io::println("ltl_chacha20 demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}