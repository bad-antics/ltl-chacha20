// ltl-chacha20/main.ltl
//
// ChaCha20 stream cipher with Poly1305 AEAD.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-chacha20 v0.1.0")
    io::println("ChaCha20 stream cipher with Poly1305 AEAD.")
    0
}
